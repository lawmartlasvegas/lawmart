# LawMart Version 1.1.1 - Navigation Fix

This maintenance release fixes the Pricing page navigation.

From `/pricing/`, the Services, Process, Why LawMart, FAQ, and Contact links now return to the corresponding sections on the homepage instead of appending anchors to `/pricing/`. The LawMart logo also returns to the homepage.

Upload all files and folders to the root of the GitHub Pages repository, replacing the existing files.
