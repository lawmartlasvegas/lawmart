const menu=document.querySelector('.menu'),links=document.querySelector('.links');menu?.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.getElementById('year')&&(document.getElementById('year').textContent=new Date().getFullYear());
const cfg=window.LAWMART_CONFIG||{};if(cfg.GA4_MEASUREMENT_ID&&!document.querySelector('script[data-ga4]')){const ga=document.createElement('script');ga.async=true;ga.dataset.ga4='true';ga.src='https://www.googletagmanager.com/gtag/js?id='+encodeURIComponent(cfg.GA4_MEASUREMENT_ID);document.head.appendChild(ga);window.dataLayer=window.dataLayer||[];window.gtag=function(){dataLayer.push(arguments)};gtag('js',new Date());gtag('config',cfg.GA4_MEASUREMENT_ID,{anonymize_ip:true});}const track=(name,params={})=>{if(typeof window.gtag==='function')window.gtag('event',name,params)};document.querySelectorAll('a[href^="https://wa.me"],a[data-track="whatsapp"]').forEach(a=>a.addEventListener('click',()=>track('whatsapp_click',{brand:'imigracao_brasileira',page:location.pathname})));document.querySelectorAll('a[href^="tel:"]').forEach(a=>a.addEventListener('click',()=>track('phone_click',{brand:'imigracao_brasileira'})));document.querySelectorAll('a[href^="mailto:"]').forEach(a=>a.addEventListener('click',()=>track('email_click',{brand:'imigracao_brasileira'})));
const gLink=document.getElementById('google-review-link');if(gLink&&cfg.IMIGRACAO_GOOGLE_BUSINESS_URL)gLink.href=cfg.IMIGRACAO_GOOGLE_BUSINESS_URL;
const form=document.getElementById('lead-form');form?.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form);track('lead_submit',{brand:'imigracao_brasileira',service:data.get('servico')||'nao_informado'});const endpoint=cfg.IMIGRACAO_FORM_ENDPOINT||cfg.FORM_ENDPOINT;if(endpoint){try{const r=await fetch(endpoint,{method:'POST',body:data,headers:{Accept:'application/json'}});if(!r.ok)throw new Error('Falha');location.href='/thank-you/';return}catch(err){alert('Não foi possível enviar automaticamente. Abriremos seu aplicativo de e-mail.')}}const body=[`Nome: ${data.get('nome')}`,`Telefone: ${data.get('telefone')}`,`E-mail: ${data.get('email')}`,`Serviço: ${data.get('servico')}`,`Mensagem: ${data.get('mensagem')}`].join('\n');location.href=`mailto:brenden@lawmartlasvegas.com?subject=${encodeURIComponent('Contato — Imigração Brasileira')}&body=${encodeURIComponent(body)}`});